[project]
name = "microscope"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "coremltools>=9.0",
    "diffusers>=0.36.0",
    "numpy<2.4",
    "torch>=2.10.0",
    "transformers>=5.2.0",
]
